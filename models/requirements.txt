fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-dotenv>=1.0.0
inference-sdk
# inference-sdk may require a specific Python version (3.8-3.11); install a compatible version
# pin a known version that supports your Python if needed, for example:
# inference-sdk==0.9.23
